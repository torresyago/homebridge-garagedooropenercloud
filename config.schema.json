
{
  "name": "Shelly Garage Door Opener Cloud",
  "domain": "homebridge-garagedooropenercloud",
  "exclusive": true,
  "type": "object",
  "properties": {
    "name": {
      "title": "Nombre",
      "description": "Nombre tal y como aparecerá en HomeKit",
      "type": "string",
      "default": "Garage Door"
    },
    "deviceId": {
      "title": "Shelly Device ID",
      "description": "ID del dispositivo Shelly (minúsculas, sin guiones)",
      "type": "string",
      "placeholder": "e868e7d2b238"
    },
    "authKey": {
      "title": "Shelly Auth Key",
      "description": "Clave de autenticación Shelly Cloud (copiar completa desde la app Shelly)",
      "type": "string",
      "format": "password",
      "placeholder": "MTAzNDEydWlk..."
    },
    "deviceType": {
      "title": "Tipo de dispositivo",
      "description": "Selecciona el origen del estado: relay (Gen1) o sensor (Plus/Gen2)",
      "type": "string",
      "enum": ["relay", "sensor"],
      "default": "relay"
    },
    "channel": {
      "title": "Canal",
      "description": "Canal del dispositivo Shelly (normalmente 0)",
      "type": "number",
      "default": 0,
      "minimum": 0
    },
    "openTime": {
      "title": "Tiempo de apertura (segundos)",
      "description": "Duración estimada de apertura para mostrar OPENING antes de pasar a OPEN",
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 300
    },
    "holdOpenTime": {
      "title": "Tiempo de estado OPEN simulado (segundos)",
      "description": "Tiempo que la puerta permanece en estado OPEN antes de iniciar el cierre automático (si está activado)",
      "type": "number",
      "default": 2,
      "minimum": 0,
      "maximum": 60
    },
    "closeTime": {
      "title": "Tiempo de cierre (segundos)",
      "description": "Duración estimada de cierre para mostrar CLOSING antes de pasar a CLOSED",
      "type": "number",
      "default": 20,
      "minimum": 1,
      "maximum": 300
    },
    "polling": {
      "title": "Polling activado",
      "description": "Habilita la consulta periódica de disponibilidad de Shelly Cloud",
      "type": "boolean",
      "default": true
    },
    "pollInterval": {
      "title": "Intervalo de polling (segundos)",
      "description": "Recomendado ≥ 60s cuando tengas varias puertas para evitar rate-limit (HTTP 429)",
      "type": "number",
      "default": 60,
      "minimum": 10,
      "maximum": 300
    },
    "httpTimeoutMs": {
      "title": "Timeout HTTP (milisegundos)",
      "description": "Tiempo máximo de espera por respuesta de Shelly Cloud",
      "type": "number",
      "default": 10000,
      "minimum": 1000,
      "maximum": 60000
    },
    "simulateOnly": {
      "title": "Modo simulación de estado",
      "description": "El estado reportado es simulado (CLOSED por defecto; OPENING→OPEN→CLOSING al abrir). El chequeo de Shelly solo verifica disponibilidad.",
      "type": "boolean",
      "default": true
    },
    "autoClose": {
      "title": "Cierre automático tras apertura simulada",
      "description": "Si está activado, tras el tiempo de apertura y el tiempo en OPEN, la puerta simulará el cierre automáticamente.",
      "type": "boolean",
      "default": true
    },
    "debug": {
      "title": "Modo debug",
      "description": "Muestra logs detallados del accesorio",
      "type": "boolean",
      "default": false
    },
    "statusCloudURL": {
      "title": "URL de estado Shelly Cloud (opcional)",
      "description": "Sobrescribe la URL por defecto si tu región usa otro endpoint",
      "type": "string"
    },
    "controlCloudURL": {
      "title": "URL de control Shelly Cloud (opcional)",
      "description": "Sobrescribe la URL por defecto si tu región usa otro endpoint",
      "type": "string"
    }
  },
  "required": ["name", "deviceId", "authKey", "deviceType"],
  "layout": [
    {
      "type": "section",
      "title": "Básico",
      "items": ["name", "deviceId", "authKey", "deviceType", "channel"]
    },
    {
      "type": "section",
      "title": "Movimiento y simulación",
      "items": ["openTime", "holdOpenTime", "closeTime", "simulateOnly", "autoClose"]
    },
    {
      "type": "section",
      "title": "Polling y red",
      "items": ["polling", "pollInterval", "httpTimeoutMs", "statusCloudURL", "controlCloudURL"]
    },
    {
      "type": "section",
      "title": "Avanzado",
      "items": ["debug"]
    }
  ]
}

